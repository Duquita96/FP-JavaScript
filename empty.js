const characters = {
    type: ["Elf", "Human", "Orc"],
  };


class WarriorInPrision {
    constructor(race, weapons) {
      this.race = race;
      this.weapons = weapons;
    }
  }
const prisioner = new WarriorInPrision(characters.type[i], 'sword');

console.log(`-atrapamos este ${this.elf} cons esta ${this.weapons}.`)